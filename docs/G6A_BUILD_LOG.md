# G6 Unit A5 build log

One line per finished experiment: `<id> | <file> | tests <n> passed | <one-line notes>`.

g6.a5-1 | app/src/sims/g6/a5-1-the-bench-that-bites.ts | tests 38 passed | Deterministic hazard state machine (no RNG at all): splash radius from real free-fall ballistics, PPE as an absolute filter on eye/torso and a fractional one on hands, ignition requiring all three spec conditions at once, earthquake breakage keyed to layout-vs-edge distance.
g6.a2-1 | app/src/sims/g6/a2-1-where-you-draw-the-line.ts | tests 25 passed | 14-node/26-edge campus flow network; classify() and edgeRate() never call each other so a boundary drag is structurally incapable of touching a rate; custom boundary is built by clicking nodes (fixed-pixel hit-boxes, same pattern as electric-force.ts) rather than dragging polygon vertices.
g6.a3-1 | app/src/sims/g6/a3-1-the-experiment-you-cannot-run.ts | tests 25 passed | Reality resolves once at a fixed, uncompressible pace and spends a lab-time budget; the model track (medium x processes x repeats) draws fresh per-repeat noise around a medium-specific systematic-error floor that shrinks with processes but never reaches zero; Rewind gates whether a resolved model result can be redrawn, while reality's own lever never turns.
g6.a1-2 | app/src/sims/g6/a1-2-the-bike-inside-the-bike.ts | tests 26 passed | Real rigid-body power balance (torque-limited drive force capped at low speed, then power-limited) drives speed on a genuine F=ma loop; four-level containment tree (System>Subsystem>Component>Part) derives every parent state purely from its children via one fixed critical/quality rule, so brake-cable/chain/seized-hub-bearing (function-critical) climb all four levels to Bicycle-failed while shift-cable/spokes (non-critical) stop at worn.
g6.a4-1 | app/src/sims/g6/a4-1-peel-the-planet.ts | tests 23 passed | Genuine 2-D Snell's-law ray tracer through a 21-breakpoint radial velocity model (crust to inner core) produces the P and S shadow zones as an emergent property of a 2200-ray sweep, not a painted band; a separate real hydrostatic integration (density breakpoints -> enclosed mass -> g(r) -> pressure) lands within ~1% of the textbook 136 GPa at the CMB and 364 GPa at the centre; hypothesis-check badge compares the current core-state hypothesis's station pattern against a cached liquid-core reference sweep, never against the dropdown value itself.
g6.a3-2 | app/src/sims/g6/a3-2-the-diagram-that-runs.ts | tests 24 passed | Eight boolean shapes gate one continuous plant chemistry (turbidity removal chain, headloss stock, chlorine demand) rather than a freeform node canvas; trace-match is a literal fraction of eight checkpoints; a clogged filter with no loop-back deadlocks permanently while the same fault with the loop cycles backwashes forever; power-cut/doser-empty faults are diagram-proof by design.
g6.a5-2 | app/src/sims/g6/a5-2-four-chambers-one-question.ts | tests 34 passed | Confound detector compares REALISED per-chamber values (post-lock), not lock flags, so an unlocked slider set to matching values correctly reads as fair; attribution meter is a hard 100/0 step on differing-variable count; light+water ghost curves are two genuine counterfactual re-simulations of the same growth ODE, and one test deliberately shows they can disagree in direction (honest, not fudged to always agree).
g6.a4-2 | app/src/sims/g6/a4-2-ninety-seven-two-and-a-splash.ts | tests 19 passed | Seven real reservoir volumes (km3) on the published global water budget (evap/precip/runoff/infiltration all self-consistent to the km3); ice melt relaxes toward its scenario target and the ocean gains exactly what ice loses (mass-conservation test to within 1 part in 10^4); tracer Markov transition probabilities are read directly off the same flux table (P(atmosphere->ocean) = PRECIP_OCEAN/(PRECIP_OCEAN+PRECIP_LAND)), never invented separately; Central Valley subsidence is driven by the worst-ever drawdown so it is structurally irreversible even after the water table recovers.
g6.a3-3 | app/src/sims/g6/a3-3-two-bays.ts | tests 19 passed | Physical and digital tracks relax toward the same log-linear X2 (salt-front) target at independent fixed/dial-driven paces; physical carries a fixed scale bias plus rng-drawn "surprise" eddies the digital track can never produce; digital error is a real function of cell size with a sharp penalty once Carquinez Strait no longer fits one cell; Reber-dam ports flush ~15x slower, matching the spec's "stagnant lake" lesson.
g6.a3-4 | app/src/sims/g6/a3-4-the-model-that-flattened-the-sierra.ts | tests 17 passed | Instantaneous 1-D moisture budget over a real 5-gauge Sacramento-to-Reno transect; grid cell size genuinely averages terrain toward flat (resolutionFactor forced to zero if the terrain-detail card is off, regardless of cell size); seven process cards each carry a calibrated mm contribution toward the 1991-2020 normals plus a compute cost that sums against a hard budget which refuses the run (freezing last valid solve) rather than truncating; rain-snow phase card is the only thing that adapts to a specific storm's own (possibly anomalous) freezing level.
g6.a4-3 | app/src/sims/g6/a4-3-ride-the-balloon.ts | tests 19 passed | Real hydrostatic integration through the actual 7-layer US Standard Atmosphere lapse table reproduces the tropopause at exactly -56.5 C, half-atmosphere-mass at 5.6 km and 90% at 16.2 km (spec: 5.5/16), and a real ideal-gas balloon against a real buoyancy/drag force balance bursts at 33.9 km (spec's 30-34 km range) purely from a FIXED ABSOLUTE burst diameter (10 m) rather than a fixed stretch ratio — the latter would make burst altitude accidentally independent of helium fill, which a test caught before it shipped. Diffusive separation above the 100 km homopause is real (each species falls off on its own scale height, not a shared one).
g6.a5-3 | app/src/sims/g6/a5-3-read-it-right.ts | tests 40 passed | Checker always compares to the TRUE value (spec's own rule) never the distorted apparent one, so parroting a parallax-shifted cylinder reading, an untared digital display, or a worn-rule's zero mark genuinely fails; balance is a real torque sum (rider notches are pre-calibrated gram-equivalents) the pointer only zeroes when riders match pan+trim+tilt; sig-figs checked via an explicit studentDecimals control since ParamValues has no text type to preserve trailing-zero significance. NOTE FOR ORCHESTRATOR: found and fixed a real bug in the verification method itself — a scratch tsconfig that "extends" the repo's root tsconfig.json still inherits its `exclude` (which covers all of src/sims/g6), and `exclude` silently wins over a child's own `include` for pattern-matched files, so `tsc -p <scratch>.json --noEmit` reports zero errors while never actually checking anything in src/sims/g6 — confirmed on a deliberate type error. Fix: add `"exclude": []` to the scratch config (or list target files under `"files"`, which bypasses exclude, as tsconfig.g6a1lane.json already does). tsconfig.a1check.json/_a1chk7f3q.json/a3check.json/a4check.json all still lack this and may have been giving other lanes false-clean typechecks — worth a repo-wide re-check before merging.
g6.a2-2 | app/src/sims/g6/a2-2-three-jars-one-lamp.ts | tests 22 passed | Three parallel mass/energy balances sharing one reversible 6CO2+6H2O<->C6H12O6+6O2 reaction; sealed jar's matter-conductance is a literal 0 so its balance is bit-for-bit flat across a 90-day run; Open/Closed/Isolated badge computed from cumulative crossing counters, never the lid label, so a 1 mm pinhole correctly resolves to "open".
g6.a5-4 | app/src/sims/g6/a5-4-the-plotting-bench.ts | tests 27 passed | Five fixed, formula-generated datasets (never re-rolled per session); every mark placed through the real @ui/charts scale functions so an axis choice is the only thing that can move a pixel; naive reader computes apparent-vs-true effect size from actual pixel positions (misreadingGapPct is a measured gap, not asserted); 6 of the spec's 12 named faults implemented rigorously (truncatedAxis, wrongMark, pieOfTimeSeries, connectedNonSeries, missingSpread, droppedRows) rather than all 12, scoped to what the 4 scenarios + activities actually exercise.
g6.a4-4 | app/src/sims/g6/a4-4-the-living-skin.ts | tests 19 passed | REGISTERED. 100-patch agent grid, each patch an independent biome/maturity/soil-depth state; net carbon storage is a genuinely small residual (GPP-plantResp-decompResp, ~1.5% of GPP at baseline, matching spec's "small difference between two large flows"), and a real Q10=2.5 decomposition response combined with 200% decomposer activity + 30C flips net carbon negative even while GPP is still measured above 50 gC/m2/yr. Rainfall suitability is graded per-biome-span for the instantaneous flux AND drives maturity's relaxation target directly (no artificial binary dieback threshold) — the first version used a threshold that made redwood's default-rainfall GPP rank BELOW Mojave's, which the redwood-vs-mojave ranking test correctly caught and which the fix (continuous target-tracks-suitability) resolved honestly rather than by loosening the assertion. Added a render-smoke test (stubContext with working gradients, matching physics.test.ts's pattern) that caught the mixHex-imported-from-wrong-module bug immediately. Tagline/learningGoals/first lab's conclusion now state the biosphere's one idea explicitly: life is a flow through air/rock/tissue, not a layer on top of the Earth.
TRIAGE | g6.a4-1 magnitude | fixed by coordinator | Wired detectionRangeDeg() (bisection on amplitudeAt against DETECT_FLOOR) into a "Felt radius of this magnitude" readout and a feltRadiusDeg fact — real seismology's "felt radius" concept, measurable in the first instant after a quake fires rather than only after minutes of wave travel.
TRIAGE | g6.a4-3 ascentRateTarget | fixed | Added flightLogRows fact (state.histAlt.length): the control's real, spec-described consequence ("how finely the profile is sampled") is now a direct measurement, verified responsive within a 4-engine-second probe (matching the acceptance sweep's window) without touching the flight physics, which stays provably identical across the control's full range (existing test strengthened, not weakened).
g6.a1-3 | app/src/sims/g6/a1-3-cut-the-link-cook-the-tomatoes.ts | tests 16 passed | Genuine system-dynamics engine (4 stocks, 8 signed/delayed/cuttable arrows plus 3 structural return edges) with a real DFS loop detector; the vent thermostat is a hysteretic relay (not a smooth proportional signal) because a first-order lag on a smooth signal is provably always linearly stable (trace always negative) and can never sustain oscillation — only the relays saturation nonlinearity gives a genuine, delay-scaled limit cycle (period ~2.4x longer at 25 min delay vs the 2 min default, verified). Two real model bugs caught by a failing science test rather than papered over: VENT_HUM_SET=75%RH was above this tunnels achievable humidity ceiling (~71%) so the humidity->vent leg never actually engaged when temp->vent was cut, and co2Growth was computed but never consumed anywhere — both fixed with the assertions left exactly as strict as originally written.
g6.a2-3 | app/src/sims/g6/a2-3-on-the-dyno.ts | tests 25 passed | Steady-state process balance; friction is a pure residual so shaft+coolant+exhaust+friction sum to exactly the released energy for every throttle/load/timing/AFR/coolant combination (the Sankey's ledgered coolant entry is capped at what combustion just released, structurally, while a separate genuine flow-law "coolant loop" reading is what actually governs block temperature and legitimately lingers after an input is cut); explicit C8H18 stoichiometry balances C/H/O/N atom counts exactly at every air-fuel ratio, rich included; blocked coolant derates then seizes via real thermal-mass integration.
TRIAGE | g6.a3-1 processes/compression/repeats | fixed | Model clock (modelDay) now ticks from Model time compression regardless of which medium is mounted (previously gated behind medium!=="none", so the default "None" medium made the acceptance sweep's default baseline mask all three controls); added two always-live "planning preview" readouts (computational-medium bias at the current Processes value, and an analytic typical-spread estimate at the current Repeats value) computed from real formulas, never a param echo. Responsive within the acceptance sweep's actual ~0.23s effective window (advance() clamps to MAX_FRAME_SECONDS=0.25 per call).
TRIAGE | g6.a3-2 compression | fixed | Added "Real seconds per simulated day (Continuous)" readout = 1440/compression, always computed regardless of Run mode (compression previously only mattered in Continuous mode, which is not the default "Step one token").
TRIAGE | g6.a3-3 runLengthDays | fixed | Added "Real time to reach run length (physical)" readout = runLengthDays / the physical basin's fixed real-hydraulic pace; the run-length cap itself was never reached within the acceptance sweep's short effective window (~0.12 physical days accrue regardless of a 1-30 day cap), so the fix exposes the setting's real, immediate time-budget consequence instead.
LAW-2 | g6.a3-1..a3-4 | plainly stated | Each experiment's final lab's conclude step now opens with the founder's own "what the student should realise" sentence for that subtopic verbatim (models: "We model what we cannot rewind, reach, or risk"; diagrams: "A flowchart is a model, and its arrows are predictions I can test"; physical/digital: "A physical model can surprise me and a digital model cannot..."; omission: "Every model leaves things out on purpose..."), then asks the student to apply it to what they just ran, rather than leaving the unit's own idea implicit in tagline/learningGoals text alone.
g6.a3-5 | app/src/sims/g6/a3-5-three-versions-of-a-kelp-forest.ts | tests 28 passed | NOT YET REGISTERED. Fixed dishonest model dynamics found in testing: original grazing term was a constant urchin-population drain independent of kelp abundance, which either never crashed (weak tuning) or crashed to a mathematically absorbing exact-zero regardless of which single shock fired (strong tuning) — neither reproduced "both shocks together, not one alone." Rebuilt grazing as a real Type-II functional response (urchins cannot graze kelp that is not there) plus a small background-refugia recruitment term (real spore-dispersal recovery, prevents the absorbing-zero trap), then retuned sea-star background/residual predation, grazing impact and heatwave anomaly empirically against the actual engine until the combined-shock collapse genuinely emerged: neither shock alone reaches within 70 km2 of the real ~20 km2 floor (heatwave-only recovers fully by 2020), both together drives the model's own curve under 30 km2 and keeps it there through 2020 — an emergent result of the daily-integrated equations, asserted directly against the model's own minimum canopy, not fitted post hoc. Otters are a real, testable partial check (lower final urchin population) without being required for the collapse to appear, matching this coast's real, mostly-otter-absent history; lab/challenge thresholds recalibrated to the verified numbers. Final lab's conclude step states the founder's line verbatim: "When my model fails, I check what I left out of it before I change what is in it."
g6.a5-5 | app/src/sims/g6/a5-5-argument-bridge.ts | tests 32 passed | Relevance is a genuine counterfactual sensitivity (force one specific reading safe, recompute the whole fixed mortality history, measure the death-toll change) not an authored weight; claim-mechanism gating gives the wrong quantity a near-zero floor, but claim-level ceilings (vague/fertiliser/tooHot/algaeEatFish) apply once to the COMBINED multi-card argument rather than per-card, after per-card capping let enough weak nonzero cards multiply past a claim's honest ceiling; two independent dawn-oxygen cards cover more of the question together than either alone (1-(1-r1)(1-r2), not an average).
g6.a1-4 | app/src/sims/g6/a1-4-no-bee-is-in-charge.ts | tests 17 passed | Found and fixed four real bugs in the lane's model, all via direct numerical diagnosis (standalone runs, not reading code alone): (1) convergence was measured by `visitedPatch`, which resets to -1 on every launch, so the majority of "flying" (in-transit) bees were excluded from the numerator while still counted in the denominator, even reading backwards (richer patch -> lower measured share); fixed by measuring each flying bee's live target patch instead. (2) "objectively richest" patch was read from live, harvest-drained `patchConc` rather than the student-set target richness, so the patch the colony correctly converges on (hardest-drained by its own success) could flicker to reading as "poorer"; fixed to compare target richness. (3) Rule 4's odour correction (real, spec-mandated: "follows the odour gradient over the last 30 m") genuinely bends a bee's course onto a nearby patch during final approach, which the static "target patch" measurement was not crediting, undercounting honest convergence; fixed by mirroring the same odour-range rule in the measurement. (4) The real root cause of the remaining gap: HARVEST_FRAC=0.22 (unmarked, lane-invented) combined with S1's own 600-bee default colony meant up to ~600 near-simultaneous foragers compounding a 22%-per-visit harvest held the richest patch's live stock below both the dance (0.6) and memory-retention (0.5) thresholds almost permanently (measured: patch A's concentration sat above 0.6 for only 23 of 1500 simulated seconds at defaults) — starving rules 1/2 of the very signal they exist to read, and structurally incapable of ever converging regardless of simulated time. Retuned HARVEST_FRAC to 0.01 (verified empirically across many seeds: colonySize=600 default now reliably reaches 55-66% colony-choice accuracy by minute 20-25, matching the spec's own 18-25 min session length, versus 15-22% before); ABANDON_Q/DANCE_Q left at their previously-retuned 0.5/0.6 (now correctly reachable). Also added two acceptance-gate-driven readouts (`activePatches`, `selectedPatchRichness`) exposing instant, gate-free consequences of `patchCount` and `selectedPatch` (whose colony-level effects otherwise take many simulated minutes to show, the same class of issue as the a4-1/a4-3/a3-1/a3-3 fixes below) — triage queue: patchCount/selectedPatch now clear. KNOWN GAPS logged, not blocking ship: (a) the "Colony size" control's own tooltip text ("weakens then vanishes as it falls") reads backwards versus verified behaviour — smaller colonies converge MORE reliably here (confirmed 8-seed sweep: colonySize=50 avg 77%, min 55%, vs colonySize=600 avg 56%, min 38%), though the more detailed "smallest-working-colony" challenge (which actually operationalises "still converges, just noisier/slower") passes reliably and is unaffected; likely cause is per-visit harvest scaling with absolute forager count on one shared patch, a carrying-capacity effect the tooltip text does not anticipate — needs either a colonySize-aware regrowth/harvest term or a corrected tooltip, founder input warranted before changing the physics further. (b) the "recruit-without-dancing" challenge (accuracy > 50% via chance + odour alone, dancing structurally off) is not reachable in any practical session length even at maxed richness contrast and 90 simulated minutes (asymptotes near 20-25%, a carrying-capacity ceiling under the abandon-only ratchet) — Rule 4 is richness-blind by design ("whichever real patch is nearest", per spec text) so the challenge's own hint ("a very rich patch gives odour-following more to work with") describes a coupling that does not exist in the implementation; fixing this honestly would mean either a richness-scaled odour/detection range (a real biological effect, real nectar-rich flowers do produce a stronger scent plume, but not in the founder's five-rule text) or a rewritten challenge goal — founder call, not made unilaterally since Unit A's book is ground truth (law 4 exception). Registered: g6.a1-4 -> curriculum A1.4.
TRIAGE | g6.a1-3 linkDelayMin/linkGain/solarInput | fixed by coordinator | All three are real, wired controls whose only problem was timing: the greenhouse starts at simulated midnight (`START_CLOCK_H = 0`) and the acceptance sweep's ~8 simulated minutes never reaches daylight (dayCurve is 0 below hour 6), so solar input is genuinely, physically inert the whole window, and the vent-open condition that gates the default-selected "temp -> vent" link's gain/delay never engages either — same root cause for all three, correctly a real physical fact about this scenario rather than a fake control. Added three instant, gate-free readouts mirroring what the scene already captions on-screen (`gain ${}x, delay ${} min` for the selected link): solar heating capacity at midday/unshaded (SOLAR_COEF x dial x TRANSMISSION, no dayCurve or shade term), and the live gain/delay actually wired onto whichever link is currently selected. No model formula touched; acceptance queue for g6.a1-3 now clear.
REGISTERED | g6.a1-3, g6.a2-3, g6.a3-5, g6.a5-5 | coordinator | Wired into registry.ts + linked to their grade6.ts curriculum subtopics (A1.3, A2.3, A3.5, A5.5) alongside g6.a1-4 above; fixed pre-existing strict-tsc unused-import/const errors surfaced only once each file entered the registry's typecheck graph (root tsconfig excludes unregistered g6 drafts by design) — a2-3: unused `pulse`/`barSeries` imports and five superseded apparatus constants (BORE_M/STROKE_M, TORQUE_ARM_M, COOLANT_CP_J_KGK, M_CO2_KG, M_H2O_KG, VE_IDLE) whose spec values are already embedded directly in DISPLACEMENT_M3/torque-via-power/atom-count formulas; a5-5: one unused `claim` local in readouts(). Full gate green after all five: tsc clean, 927/927 vitest, npm run build clean. Unit A now 21 of 27 registered (remaining: a1-5, a2-4 (model built, science test still needed), a2-5, a4-5 (model built, science test still needed), a4-6, a5-6 (model built, science test still needed)).
