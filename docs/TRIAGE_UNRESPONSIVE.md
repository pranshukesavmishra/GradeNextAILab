# Triage: unresponsive controls

Companion to docs/QUALITY_STATUS.json — one row per dead control from the acceptance sweep, with its root-cause class ((a) never wired, (b) effect unmeasured, (c) intended null result, (d) invisible at defaults), the fix applied, and verification status.

| Sim | Control | Class | Root cause | Fix | Verified |
| --- | --- | --- | --- | --- | --- |
| phys.motion-graphs | drive | d | The dial is only read when mode != "script", and the default mode is "script", so the demanded velocity always came from the script. | The speed dial now takes over from the script whenever it is off zero (and hands back at zero), with the on-stage mode label and help text reflecting the takeover. | yes |
| phys.motion-graphs | observerSpeed | d | Only read when viewFrame = "observer"; at the default "ground" frame nothing consumed it. | Added the honest "Velocity the observer measures" readout (m/s) and vObserved fact, valid in both frames, and the observer is now drawn walking the bench in the ground frame too. | yes |
| g6a5-swing-and-graph | mass | c | Intended null result — the small-swing period T = 2π√(L/g) is mass-independent, and by design mass fed no measurement at all, so the control looked fake instead of teaching the null. | Period physics untouched; measure() now also returns bobWeightN and swingEnergyMillijoules (both ∝ mass), so students see mass move weight and energy while every timing column holds still — the null effect on the period is the lesson. | yes |
| g7d3-cycles-and-flows | transferPercent | d | The trophic-steps control defaults to 0 meals, and transferPercent^0 = 1, so at the default the transfer efficiency multiplies nothing. | Default steps is now 1 meal, so the energy account responds to the efficiency immediately (energyRemainingPercent = transferPercent at one step) while the carbon account still balances at 100%. | yes |
| g7f2-ten-times-rarer | bValue | d | The Gutenberg-Richter line 150·10^(−b·(M−6)) pivots at the M6 anchor and the default magnitude was exactly 6, so b multiplied an exponent of zero. | Default magnitude is now 7, off the pivot, so the b-value moves eventsPerYear, yearsBetweenEvents and the 30-year chance immediately (the M6 pivot is still discoverable by sliding magnitude back). | yes |
| phys.electric-force | chargeRate | d | The charging rate only feeds the storm scenario's cloud, and the default scene is the bench, where step() returns before the storm block, so no measurement ever consumed it. | Added the honest predicted "Seconds between strikes" (breakdownCharge ÷ rate) as a storm readout and an always-exposed strikeIntervalS fact, mirroring the existing scenario-independent breakdownCharge fact; in the storm itself the rate already visibly paces real strikes (~2 s apart at defaults). | yes |
| phys.em-spectrum | zoom | b | Zoom is a real viewport control (it rescales the ruler, bands and drawn wave) but its effect lived only in render — no readout or fact measured anything about the view. | Added the honest "Of the screen that is visible light" percent readout and visibleShare fact (overlap of the 380–700 nm band with the on-screen decade window ÷ window width) — the exact "how narrow was it" number the zoom exercise teaches — plus the same share printed in the on-stage caption. | yes |
| chem.ph | titrantConc | d | Defaults made both of its causal paths vanish: volumeAdded = 0 means the concentration multiplies zero, and acid-into-lemon-juice (acid into an acid) has no equivalence point, so equivalenceVolume was 0 for any concentration. | Default titrant is now the base — the natural neutralisation of the default lemon-juice flask — so titrantConc sets a real equivalenceVolume/equivalenceError before the first drop and shapes the pH curve after it; the burette callout now prints the actual concentration instead of a hardcoded "0.1 M". | yes |
| bio.artificial-selection | keep | b | The breeding fraction only acts when a generation turns over (every 1 sim-second, past the harness's clamped 0.25 s window), yet the render already computes and draws its live effect — the breeding cut line and circled parents — which no readout or fact measured. | Added the honest live "Breeding cutoff" readout plus breedingCutoff and nextSelectionDifferential facts (phenotype of the worst kept parent, and how much better than average the chosen parents currently are), computed by the same cut the histogram draws. | yes |
| bio.cell | light | d | Light only feeds chloroplasts (made = chloroplasts × rate × light) and the default cell was the animal cell, which has none — so the slider fed nothing, exactly as its own help text admits. | Default cellType is now "plant": light immediately drives glucose → ATP, O₂ and CO₂ through the photosynthesis/respiration chain (no light, no glucose, no ATP), and switching to the animal cell turns the null result into a discoverable lesson rather than the opening experience. | yes |
| bio.fossil-record | fossilRate | d | The tracked record's earliest group (trilobites) first appears at 521 Ma but the run started at 540 Ma, so the opening stretch deposits only barren rock — nothing alive means nothing buried at any burial rate. | Default startMa is now 520 Ma, where the tracked record begins, so the burial rate immediately moves fossilCount and the deepest/shallowest fossil dates (the 540 Ma "Cambrian" mark stays on the slider and lab setups still pin 540). | yes |
| bio.homology | align | d | autoAlign defaults on, and while it is on the model replaces align with its own cosine sweep, so the slider was ignored at the default configuration. | The slider now takes over from the sweep whenever it is off zero (returning it to zero resumes the sweep), so the Alignment readout and align fact follow the student's hand; autoAlign help text says so. | yes |
| bio.human-impact | protectLand | b/c | The model is honest — protection only bites when farm demand would eat protected land — but at the default 8 B population farming claims 46% of land, so protection ≤ 50% never binds and habitat = max(protect, 0.538) is constant across the whole slider; the guarantee the slider genuinely buys was unmeasured. | Added the honest "Species secured by protection" readout plus protectedFraction and speciesFloorFraction facts — the species-area floor speciesRemaining(protect) that the protected land alone sustains even if farming swallowed every free hectare (the 30x30 argument); the live habitat/species numbers still show the null until farming pressure makes protection bind, which is the lesson. | yes |
| bio.natural-selection | mutationRate | b | Mutation only acts at generation boundaries (every 1.2 sim-seconds, past the harness's clamped window) and even then nothing counted it — allele flips vanished into the population with no measurement of mutation itself. | Added a real cumulative mutants counter in state (babies born with a flipped fur gene, exposed as the mutantBirths fact) plus the honest live "New variants expected per generation" readout and expectedMutantsPerGeneration fact (population × offspring × rate) — the "why mutation alone is slow" number from the learning goals. | yes |
| earth.spheres | rate | d/b | The Speed dial scales the event clock, but the first sphere-to-sphere transfer fires at 2.4 event-seconds — outside the observation window — and the paced clock itself was pure hidden state, so no measurement responded at any speed. | Added the honest "Next transfer in (s)" countdown readout ((STEP_SECONDS − t mod STEP_SECONDS) ÷ rate, 0 once complete) plus eventTime and nextTransferS facts, so the dial visibly and measurably paces the walk from the first tick. | yes |
| earth.water-cycle | vegetation | b | Vegetation only set the soil→plant branching probability of the single tracked molecule — a stochastic path effect many dwell-times beyond the observation window, never measured as a flux. | Extracted the shared transpirationShare(veg) = 0.35 + 0.4·veg helper (the molecule's soil branch and the new measurement draw on the same number) and added the honest "Transpiration (thousand km³/yr)" readout plus transpiration and transpirationShareOfSoil facts: LAND_ET × share × sunPower, ≈43,000 km³/yr at defaults, matching the accepted global estimate. | yes |
